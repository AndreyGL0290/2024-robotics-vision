
class NoteConstants:
    # Color range of a note
    color_range = (
        (0, 100, 100),
        (30, 255, 255)
    )
    
    # Amount of colors that initial frame will be segmented into
    kColors = 5